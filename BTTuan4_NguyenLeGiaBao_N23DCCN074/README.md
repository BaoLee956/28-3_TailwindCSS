# Ghi Chú Cá Nhân

Project Vite + React (JavaScript) cho bài tập "Ghi Chú Cá Nhân".

Chạy cài đặt và khởi động dev server:

```bash
npm install
npm run dev
```

Thư mục chính: `src` chứa `main.jsx`, `App.jsx`, `context/ThemeContext.jsx`, `components/*` và `index.css`.
